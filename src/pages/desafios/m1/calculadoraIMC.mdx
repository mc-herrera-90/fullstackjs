<FloatingToC/>

<ProjectDetails 
  details={{
    name: "Calculadora IMC",
    abstract: `Este proyecto consiste en hacer una __aplicación de consola en Java__ que calcule el IMC ( Índice de Masa Corporal ) a partir del \`peso\` y la \`altura\` que ingresa el usuario por teclado. La idea es practicar conceptos básicos de programación orientada a objetos, como el uso de clases, métodos y entrada de datos, manteniendo el código ordenado y fácil de entender.`,
    languages: "Java",
    ide: "VS Code",
    javaVersion: `Java (\`latest version\`)`,
    type: "Aplicación de consola",
    recommended: "Java Básico",
    time: "15 mins – 30 mins"
  }}
/>

El __IMC__, conocido como el índice de masa corporal, es una medida que asocia el peso de una persona. Este valor es utilizado como indicador nutricional y constituye un índice fácil y sencillo de calcular para determinar el estado de obesidad y sobrepreso de una persona.

El __IMC__ se calcula de la siguiente manera:

<MathComponent formula="\\IMC=\frac{W}{H^2}" />

__Donde:__

- `peso` está en __kilogramos (kg)__
- `altura` está en __metros (m)__

Para ello la Organización Mundial de la Salud (OMS) ha determinado una clasificación para distintos rangos de valores:

|IMC|Clasificación OMS|
|:--|:----------------|
|`< 18.5`|Bajo Peso|
|`18.5 - 25`|Adecuado|
|`25 - 30`|Sobrepeso|
|`30 - 35`|Obesidad Grado I|
|`35 - 40`|Obesidad Grado II|
|`> 40`|Obesidad Grado III|

## PASO 1 Crear el archivo y la clase principal

- Primero, abre __Visual Studio Code__.
- Selecciona o crea una carpeta donde guardar el proyecto, ej: `calculadora-imc`
- Crea un nuevo archivo llamado `CalculadoraIMC.java`.

Este será el punto de entrada al programa:

```java
// filename: CalculadoraIMC.java
public class CalculadoraIMC {
    public static void main(String[] args) {
        // Aquí irá el código principal
    }
}
```

<Blockquote type="warning">
Recuerda que en Java, el nombre del archivo debe coincidir exactamente con el nombre de la clase pública principal.  
</Blockquote>

## PASO 2 Capturar los datos del usuario

Java no trae por defecto entrada desde consola, por eso es que se necesita importar la clase `Scanner` desde la librería `java.util`.

Vamos a usar `Scanner` para leer el __peso__ y la __altura__ por teclado.

```java
// filename: CalculadoraIMC.java
import java.util.Scanner;

public class CalculadoraIMC {
    public static void main(String[] args){
        Scanner scanner = new Scanner(System.in);

        System.out.println("Ingresa tu peso en kg: ");
        double peso = scanner.nextDouble();

        System.out.println("Ingresa tu altura en metros: ");
        double altura = scanner.nextDouble();
    }
}
```

## PASO 3 Calcular el IMC

A continuación, aplicamos la fórmula matemática:

```java
// filename: CalculadoraIMC.java
public class CalculadoraIMC {
    public static void main(String[] args){
        // código anterior...
        double imc = peso / (altura * altura);
    }
}
```

Luego, mostramos el resultado obtenido, pero limitándolo a 2 decimales:

```java
// filename: CalculadoraIMC.java
public class CalculadoraIMC {
    public static void main(String[] args){
        // código anterior...
        double imc = peso / (altura * altura);
        System.out.printf("Tu IMC es: %.2f\\n", imc);
    }
}
```

## PASO 4 Clasificar el resultado

Ahora agregamos la lógica para mostrar la categoría según el resultado:

```java
// filename: CalculadoraIMC.java
public class CalculadoraIMC {
    public static void main(String[] args){
        // código anterior...
        double imc = peso / (altura * altura);
        System.out.printf("Tu IMC es: %.2f\\n", imc);
        
        if (imc < 18.5) {
            System.out.println("Clasificación: Bajo peso");
        } else if (imc < 24.9) {
            System.out.println("Clasificación: Normal");
        } else if (imc < 29.9) {
            System.out.println("Clasificación: Sobrepeso");
        } else {
            System.out.println("Clasificación: Obesidad");
        }
    }
}
```

## PASO 5 Repositorio

Pasa por el repositorio, ahí encontrarás el código completo por si quieres revisar la solución o clonarlo para experimentar por tu cuenta.

<GitHubRepo
  owner="mc-herrera-90"
  repo="calculadoraIMC_Java"
/>
