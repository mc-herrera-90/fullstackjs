<ProjectDetails 
  details={{
    name: "Cachipun",
    abstract: `Este proyecto consiste en crear una __aplicación de consola en Java__ que simule el juego de __Cachipún__ ✊📄✂️ (piedra, papel o tijera) contra la computadora 🤖. El usuario debe ingresar su jugada por __teclado__, y el programa responderá con una **elección aleatoria**, determinando al **ganador** según las reglas del juego.
    
    Ideal si estás empezando con Java, este ejercicio te ayudará a practicar __entrada de datos__, __condicionales__ y __generación aleatoria__.
`,
    languages: "Java",
    ide: "VS Code",
    javaVersion: `Java (\`latest version\`)`,
    type: "Aplicación de consola",
    recommended: "Java Básico",
    time: "15 mins – 30 mins"
  }}
/>


El clásico **Cachipún**, consiste en un enfrentamiento de dos jugadores, y cada uno elige una opción.

__Reglas básicas__:

* El usuario elige entre `piedra`, `papel` o `tijeras`.
* El computador elige una opción aleatoria.
* Gana quien siga la lógica del juego:
    - 🌑 Piedra vence a tijeras  
    - ✂️ Tijeras vencen a papel  
    - 📄 Papel vence a piedra


## PASO 1 Crear el archivo y la clase principal

- Primero, abre __Visual Studio Code__.
- Selecciona o crea una carpeta donde guardar el proyecto, ej: `cachipun-java`
- Crea un nuevo archivo llamado `Cachipun.java`.

Comienza con la estructura mínima e importamos las clases necesarias:

```java
// filename: Cachipun.java
import java.util.Scanner;
import java.util.Random;

public class Cachipun {
    public static void main(String[] args) {
        // Aquí va el código
    }
}
```

> Ya conoces `Scanner`, que sirve para leer lo que escribe el usuario por teclado. También importamos la clase `Random`, que usaremos para generar la jugada aleatoria del computador (piedra, papel o tijera), simulando así una elección impredecible como en un juego real.

## PASO 2 Entrada del usuario y jugada aleatoria

El siguiente paso es capturar la jugada del usuario y generar el de la computadora al azar (utilizando `Random`):


```java
// filename: Cachipun.java
import java.util.Scanner;
import java.util.Random;

public class Cachipun {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();

        System.out.println("Bienvenido al juego de Cachipún");
        System.out.print("Elige una opción (piedra, papel o tijera): ");
        String jugador = scanner.nextLine().toLowerCase();

        String[] opciones = {"piedra", "papel", "tijera"};
        String computadora = opciones[random.nextInt(3)];

        System.out.println("La computadora eligió: " + computadora);
    }
}
```


> __En este bloque__:
> ```java
> String[] opciones = {"piedra", "papel", "tijera"};
> ```
> - Creamos un *arreglo* (o array) de tipo `String` que contiene las posibles jugadas. Es una forma práctica de tenerlas todas en una sola variable.  
>
> __En este bloque__:
> ```java
> opciones[random.nextInt(3)]
> ```
> - Genera un número aleatorio entre `0` y `2`. Ese número se usa como índice para elegir una opción del arreglo `opciones`, simulando así la jugada aleatoria de la computadora.


## PASO 3 Determinar al ganador

Comparamos las opciones y mostramos el resultado:

```java
// filename: Cachipun.java
import java.util.Scanner;
import java.util.Random;

public class Cachipun {
    public static void main(String[] args) {
        // ... Código anterior
        if (jugador.equals(computadora)) {
            System.out.println("¡Empate!");
        } else if (
            (jugador.equals("piedra") && computadora.equals("tijera")) ||
            (jugador.equals("papel") && computadora.equals("piedra")) ||
            (jugador.equals("tijera") && computadora.equals("papel"))
        ) {
            System.out.println("¡Ganaste!");
        } else if (
            jugador.equals("piedra") ||
            jugador.equals("papel") ||
            jugador.equals("tijera")
        ) {
            System.out.println("¡Perdiste!");
        } else {
            System.out.println("Opción no válida. Debes elegir piedra, papel o tijera.");
        }

        scanner.close(); // Cerramos el Scanner
    }

}
```

> 📌 **Nota importante sobre `.equals()` en Java**
>
> En Java, **no debes usar `==` para comparar cadenas de texto**, ya que `==` compara si dos objetos son el mismo en memoria, no si contienen el mismo texto.
>
> Para comparar el contenido de dos cadenas, se utiliza el método `.equals()`, así:
>
> ```java
> "piedra".equals("piedra") // true
> "piedra".equals("tijera") // false
> ```
>
> En nuestro juego de Cachipún, usamos `jugador.equals(computadora)` para saber si el usuario y la computadora eligieron la misma opción.
>
> También usamos condiciones como:
>
> ```java
> jugador.equals("piedra")
> ```
>
> para verificar que el texto ingresado por el usuario coincide exactamente con una de las opciones válidas.

## PASO 4 Repositorio

Pasa por el repositorio, ahí encontrarás el código completo por si quieres revisar la solución o clonarlo para experimentar por tu cuenta.

<GitHubRepo
  owner="mc-herrera-90"
  repo="cachipun-java"
/>